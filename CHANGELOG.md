# 变更日志 · CHANGELOG

记录本站在 2026 年纳新季的优化内容。

## 手机端适配
- 横屏守卫（`landscape.js`）：移动端竖屏强制横屏；访问提示弹窗（`visit-tip.js`）仅主站且仅移动端弹出。
- 触屏点按目标 ≥40px；刘海屏安全区适配；触屏取消 hover 粘滞；iOS 输入框防缩放。
- 手机端各尺寸（320/360/390 竖屏、844 横屏）审计：无横向溢出、无过小文字/点按。
- 主站地址栏主题色随栏目切换（浅色栏目浅、深色栏目深）。

## 性能
- 活动栏目背景视频（约 17MB）改为「进入活动栏目才加载/播放」，首屏不再下载。
- 纳新人物卡图片启用 `loading="lazy"`。

## 基础与可访问性
- 全站统一 `favicon`、`theme-color`、各页 `meta description`。
- FLAG 输入框补充 `aria-label`；图片均含 `alt`。

## 兼容性
- 所有移动端规则均在媒体查询内，桌面端经逐像素对比**零影响**。
- 24 组（8 页 × 桌面/平板/手机）回归测试通过，内部资源无 404。

## 清理
- 移除未被引用的遗留资源（demo 样式、旧背景图、QQ 图标）。
## 追加优化（性能 · 横屏）
- 关于页背景图（约 948KB）改为进入该栏目才加载；活动视频（约 17MB）进入栏目才播放；人物卡图片 lazy——首页首屏由 1667KB 降至约 720KB。
- lottie 动画库本地化（`lib/lottie/lottie.min.js`），离线也能显示 Logo 动画，CDN 作兜底。
- 横屏矮屏：各栏目侧边导航固定到屏幕底部、子页面留白与字号收紧。
- 全站确认使用相对路径，兼容 GitHub Pages 子路径部署。
## 追加（内容与修复）
- 加入页：手机号支持点击复制与一键拨号、微信群二维码、加入引导、**入社流程（三步）**、**常见问题 FAQ（折叠）**。
- 关于页：新增**社团亮点标签**（实战训练 / 竞赛资源 / 学长带教 / 实验靶场）。
- 活动视频：新增封面 poster、增强移动端自动播放兼容、点击兜底播放、隐藏时 `display:none` 避免个别浏览器穿透。
- SEO/PWA：各页 Open Graph 分享卡片与封面图、JSON-LD 结构化数据、`.nojekyll`、根 favicon。
- 子页面：新增“回到顶部”按钮。

## 追加（视频播放兼容性加强）
- 活动视频：`.research-screen__bg` 增加封面图 CSS 兜底，自动播放失败时不再黑屏。
- 活动视频：补充 `x5-*`（QQ/UC/部分百度内核）内联播放属性与 `disablepictureinpicture`；`canplay`/`loadeddata` 重试 + 标签页重新可见时续播。
- 手势粒子摄像头视频：未进入该栏目时 `display:none`，避免个别浏览器（如百度）隐藏视频穿透遮挡页面。

## 部署（2026-09-23）
- 最新构建已发布至 Cloudflare Pages 项目 `safe`（生产域名 `safe-1r9.pages.dev` 与自定义域名 `safe.zain-dev.top`）。
- 修复 GitHub 仓库与 Cloudflare Pages 的自动部署授权，push 到 `main` 即自动构建发布。

## 移动端问题修复（2026-09-23）
- 初始界面 Logo 丢失：`.logo-stage` 子元素全为绝对定位，在手机单栏（竖屏）与矮横屏布局下塌缩为 0，导致 Lottie 标志不可见。为手机各断点显式设定 Logo 容器尺寸（竖屏 `min(22rem,88vw)`、横屏 `min(16rem,44vh)`）。
- 活动栏目「查看纳新安排」在手机上只播动画、无内容：移动端浏览器拦截 `window.open` 弹窗。改为新标签失败时回退当前页跳转 `training.html?from=research`。
- 手机端自动横屏：横屏遮罩新增「自动横屏」按钮，调用全屏 + `screen.orientation.lock('landscape')`（Android Chrome 生效；iOS 不支持时仍提示手动旋转）。
- 修复 X5 内核（QQ/UC/百度/微信）在点「进入」时把活动视频（木星）劫持为全屏播放：不再在「进入」手势中自动请求全屏；活动视频离开栏目后移除 `autoplay` 并在未进入活动栏目时 `display:none`；X5 内核下自动横屏退化为手动旋转提示。
- 活动视频增加兜底：任何情况下只要不在活动栏目被播放，立即暂停并移除 `autoplay`。
- 静态资源加版本号（`?v=`）破除 4 小时缓存（Cloudflare `max-age=14400`），确保更新后手机端及时生效。
- 移动端活动栏目改为**仅封面图**：QQ/UC/百度等 X5 内核无法内联播放 `<video>`，会弹出原生全屏播放器且需手动退出；现移动端完全不加载/不播放该视频，只显示封面（桌面端仍正常播放）。同时移除横屏守卫中所有 `requestFullscreen` 调用，仅保留手动旋转提示，杜绝全屏副作用。
- 活动栏目封面换成从活动视频中截取的行星帧（横版 `research_poster.jpg`、竖版 `research_poster_mobile.jpg`），替换原先近乎空白的星空图，手机端背景不再空荡。
- 修复入口页 Logo 动画（`rhine-logo-animation-data.js`）末尾残留白块：装饰矩形横向动画结束帧未完全移出画布，停在右下角形成小白块；将末帧终点右移使其完全离屏，并为该脚本加版本号 `?v=20260923e` 破除缓存。

## 桌面端问题修复（2026-09-23）
- 修复桌面端切换栏目时「先跳到活动栏目再跳回来」的闪烁：屏幕切换用 0.9s 透明度淡入，而「活动」屏幕一旦访问过就始终带着 `is-visible` 残留在底层（旧的 `show*` 只移除部分屏幕），淡入期间它便露出来。新增 `hideAllScreens()`，每次切换前先清掉所有其它屏幕的可见态并暂停背景视频，彻底消除残留。
- 桌面端侧边导航「方框」放大美化：字号 0.9rem→1.05rem，毛玻璃面板内边距、间距、圆角、最小宽度与模糊/阴影同步增强（关于 / 活动 / 互动三栏的 `--light` 面板及首页 / 纳新 / 方向栏目的间距）。移动端规则不受影响。
- 变更的 CSS 与 `enter.js` 统一加版本号 `?v=20260923f` 破除 4 小时缓存。

## 移动端自动横屏修复（2026-09-23）
- 「自动横屏」按钮此前无效：`requestLandscape()` 只调 `screen.orientation.lock()` 而不请求全屏，浏览器因未处于全屏而一律拒绝，点击后永远退化为「请手动旋转手机」。现改为**先全屏、再锁定横屏方向**，标准内核（Chrome / Firefox / 三星等）可真正自动旋转。
- 全屏前先暂停并隐藏页面内所有 `<video>`、退出遮罩时恢复，防止 QQ/UC/百度等 X5 内核把视频提升为原生全屏播放器而劫持页面。
- iOS Safari 不支持方向锁，仍保留「请手动旋转手机」提示。`landscape.js` 加版本号 `?v=20260923g` 破除缓存。
- 修正：暂停/隐藏视频仍挡不住 X5 内核劫持——其只要检测到页面存在 `<video>`，请求全屏时就会弹出原生播放器。故改为 **X5 内核直接跳过全屏**，仅提示手动旋转（不再劫持）；标准内核仍自动横屏。`landscape.js` 版本号更新为 `?v=20260923h`。
- 再修正：小米/OPPO/vivo/华为等**手机自带浏览器**同属 X5 系内核但未被正则识别，仍被劫持。改为**全屏前把所有 `<video>` 整个移出 DOM**（隐藏 `display:none` 无效），X5 找不到视频即无法劫持；移动端活动视频本就仅显示封面，移除零副作用。退出遮罩时按原位置放回，自动横屏对所有内核恢复尝试。`landscape.js` 版本号更新为 `?v=20260923i`。
- 最终决定**删除自动横屏功能**：各厂商浏览器对全屏/方向锁支持参差且易劫持视频，收益差、风险高。`landscape.js` 精简为纯提示遮罩（竖屏时提示「请将手机横屏观看」，横屏自动放行），移除全屏、方向锁、视频移出/恢复及 X5 判定等全部逻辑。`landscape.js` 版本号更新为 `?v=20260923j`。

## 多机型屏幕比例适配（2026-09-24）
- 统一「受限视口」判定：`max-width:1100px` / `max-height:720px` / `max-aspect-ratio:3/2` 三种情况一律让各栏目成为可滚动容器，竖屏与近方形平板（4:3、3:2）不再出现内容被裁、按钮点不到。
- 方向页 / 活动页：绝对定位的卡片与面板在平板宽度（≤1600px）下会互相压住，统一改为可滚动的流式布局。
- 首页：矮屏笔记本（1366×768、1536×864 等）改为两栏并允许滚动，装饰区 `homepage-visual` **保持原样**（尺寸/射线/方块均未改），底部「进入互动体验」按钮不再被裁。
- 进入页：单栏断点由 980px 提高到 1100px，1024 等宽度下 Logo 不再压住文案。
- 侧边栏目导航：窄屏 / 矮屏 / 平板上统一改为**固定到屏幕右侧、竖排**（取代此前的底部横条 / 中间横排），不再占用纵向空间把内容挤下去。
- 为右侧导航预留空间：成员 / 方向 / 活动 / 关于内容加 `padding-right`；单栏首页同步预留，标题不再顶到导航；`homepage-visual` 未做任何改动。
- `landscape.js`：尺寸读取多来源兜底、resize 防抖到下一帧、移除 `scrolling="no"`（子页面可正常滚动）；补充「小屏 + 多点触控」移动端判定。
- 全局：`text-size-adjust:100%` 防移动内核放大字号；`body` 横向溢出兜底。
- 变更资源版本号统一更新为 `?v=20260924b`。
- 以无头 Chrome 在 16:9 / 20:9 / 21:9 / 折叠屏 / iPad / 1024·1180·1366·1440·1536·1600·1920·2560·3440 等 19 组视口对全部栏目与 8 个子页面做了溢出/裁切/重叠审计：正文与按钮均可见、右侧导航不遮字。

## 子页面与窄屏对齐（2026-09-24 追加）
- 跳转子页面（方向 / 加入 / FLAG / 学习训练 / 荣誉 / 提示 / 404 / 问卷）统一适配：品牌标记宽度改为贴合内容，不再与右上角导航争位；横屏矮屏与近方形平板下品牌改为随页面滚动。
- 子页面 `direction.css` 补上 `max-aspect-ratio:3/2` / `max-height:720px` 紧凑断点，卡片在平板/矮屏下自动居中收边，`?v=20260924c` 破除缓存。
- 很窄的手机横屏（视口宽 ≤760px）：首页文案、方向卡片、活动面板统一改为**单栏满宽对齐**——方向每组最后一张卡横跨两列消除半行空位，活动计划面板与信息卡上下整行排列，首页文案与装饰区居中叠放；右侧给导航预留空间，正文不被遮挡。
- `mobile.css` 版本号更新为 `?v=20260924d`。
- 复核：窄屏（568/640/667）与宽屏（915/1024/1180/1366/1440/1536/1600/1920/2560/3440）全部栏目与 8 个子页面均无横向溢出、无裁切、无正文被导航遮挡。